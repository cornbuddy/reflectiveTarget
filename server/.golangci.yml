---
version: "2"

formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/cornbuddy/reflectiveTarget/server

linters:
  settings:
    errcheck:
      exclude-functions:
        # this method never returns error, see the source code
        - (*database/sql.Rows).Close
    gocognit:
      min-complexity: 20
    goconst:
      numbers: true
    gocyclo:
      min-complexity: 20
    gosec:
      excludes:
        # TODO: enable it once hits the cloud. I test it with http, so
        # skipping it for now
        - G124
        # I rely on random only in tests, and crypto/rand has ugly api
        - G404
    nakedret:
      max-func-lines: 0
    nestif:
      min-complexity: 3
    paralleltest:
      check-cleanup: true
    testifylint:
      enable-all: true
      disable: [encoded-compare]
      go-require:
        ignore-http-handlers: true
  enable:
    - bidichk
    - copyloopvar
    - decorder
    - dupl
    - durationcheck
    - embeddedstructfieldcheck
    - err113
    - errcheck
    - errname
    - errorlint
    - funcorder
    - gocheckcompilerdirectives
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - gosec
    - gosmopolitan
    - govet
    - iface
    - interfacebloat
    - intrange
    - iotamixing
    - misspell
    - mnd
    - modernize
    - nakedret
    - nestif
    - nilerr
    - nilnesserr
    - nlreturn
    - noctx
    - nolintlint
    - nonamedreturns
    - paralleltest
    - perfsprint
    - reassign
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - testifylint
    - testpackage
    - unconvert
    - unparam
    - unqueryvet
    - unused
    - usestdlibvars
    - usetesting
    - wastedassign
    - whitespace
