server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    location /static {
        rewrite ^/static/(.+)$ /$1 break;
        root /usr/share/nginx/html;
    }

    location / {
        proxy_pass http://server:8080/;
    }
}

