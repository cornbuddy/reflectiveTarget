services:
  client:
    image: nginx:latest
    volumes:
      - ../client:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on: [server]
    ports:
      - 8080:80
  server:
    build:
      context: ../server
